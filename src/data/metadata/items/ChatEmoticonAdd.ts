export class ChatEmoticonAdd {
    public id: number;
    public duration: number;

    public constructor(id: number, duration: number) {
        this.id = id;
        this.duration = duration;
    }
}
