export class SelectItemBox {
    public groupId: number;
    public boxId: number;

    public constructor(groupId: number, boxId: number) {
        this.groupId = groupId;
        this.boxId = boxId;
    }
}
